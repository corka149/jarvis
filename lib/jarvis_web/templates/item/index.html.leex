<%= f = form_for @changeset, "#", [phx_submit: :save] %>
  <%= hidden_input f, :id %>

  <%= label f, :name %>
  <%= text_input f, :name %>
  <%= error_tag f, :name %>

  <%= label f, :amount %>
  <%= number_input f, :amount %>
  <%= error_tag f, :amount %>

  <%= submit "Save", class: "btn green" %>
  <span><%= link gettext("Back"), to: Routes.shopping_list_path(@socket, :index), class: "btn orange"  %></span>
</form>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Amount</th>
      <th>&nbsp;</th>
    <tr>
  </thead>

  <tbody>
    <%= for item <- @items do %>
      <tr>
        <td><%= item.name %></td>
        <td><%= item.amount %> </td>
        <td>
          <button phx-click="edit/<%= item.id %>" class="btn-floating btn-large waves-effect waves-light orange">
            <i class="material-icons right">edit</i>
          </button>
          <button phx-click="delete/<%= item.id %>" class="btn-floating btn-large waves-effect waves-light red">
            <i class="material-icons right">delete</i>
          </button>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
