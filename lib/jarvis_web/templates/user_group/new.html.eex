<h1><%= dgettext "accounts", "New user group" %></h1>

<%= render "form.html", Map.put(assigns, :action, Routes.user_group_path(@conn, :create)) %>

<span><%= link gettext("Back"), to: Routes.user_group_path(@conn, :index), class: "btn orange" %></span>
