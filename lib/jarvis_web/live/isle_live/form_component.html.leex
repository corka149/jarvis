<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
    id: "isle-form",
    phx_target: @myself,
    phx_change: "validate",
    phx_submit: "save",
    class: "pure-form pure-form-aligned" %>

  <fieldset>
    <!-- NAME -->
    <div class="pure-control-group">
      <%= label f, :name %>
      <%= text_input f, :name %>
      <%= error_tag f, :name %>
    </div>
    <div>
      <%= save_button() %>
    </div>
  </fieldset>
</form>
